User:
Atlas.docx
Чекпоинт 1.docx
Чекпоинты.docx
Это все файлы сессий по нашему проекту. Восстанови весь контекст( без повторения) И давай продолжим с последней точки файла чекпоинты

Kimi:
🧠 Atlas: восстановление контекста
Что такое Atlas (сейчас)
Atlas Digital Studio — локальная мультифункциональная система на Python, использующая Ollama (Qwen 3B/7B) + облачные API (Gemini, Cerebras, Groq, OpenRouter, Cloudflare, HuggingFace) для создания цифровых продуктов. Железо: GTX 1650 Ti 4GB. Философия: «Не изобретай велосипед» — использовать готовые open-source решения.
Архитектура
Atlas/ → Agents/ (executive, brief, developer, image_generator, branding_agent, media_agent, product_router, auto_skill_hunter), Config/ (llm_client.py — Model Router, models.yaml), Skills/ (web: modern_landing, agency, product_showcase, motion_premium; + заготовки games/apps/branding/media), Projects/, Memory/, Tools/. GitHub-репо: koghuhdlageneratorarf-eng/Atlas.
Построено (работает)
Гибрид моделей: Executive/Self-Upgrade → 7B (или облако), Developer → 3B.
Skills-first: Developer заполняет HTML-шаблоны, не пишет с нуля.
Auto-AOS: автовставка анимаций скролла в любой сайт.
Image Generator: три режима — [stock] (Unsplash), [ai] (FLUX), fallback (Pollinations).
Motion Premium skill: GSAP + Lenis + glassmorphism + bento grid + magnetic buttons + spotlight.
Brief Agent: два режима — короткий запрос (<300 симв.) генерирует ТЗ, длинный использует как ТЗ. Генерирует image tasks.
AutoSkill Hunter: ищет шаблоны на GitHub, анализирует LLM, адаптирует пути, ставит в Skills/.
GitHub-интеграция: код на репозитории, передача контекста через ссылку.
Model Router: 7 провайдеров, авто-fallback, ~2 млн токенов/день бесплатно.
В резерве (заготовки, не развивать до завершения web)
Branding Agent (SVG-логотип, brand book HTML).
Media Agent (AI-фотосессия + HTML-альбом).
Product Router (определяет тип: web/game/app/branding/media).
Последняя точка (из Чекпоинты.docx)
Обсуждались два направления: 1. Автономный бизнес-пайплайн: Atlas сам находит клиентов (парсинг Avito/Услуги), формирует КП, ведёт переписку (с твоим одобрением), создаёт продукт, деплоит, выставляет счёт. Этапы: ручные продажи → авто-лиды → авто-переписка → авто-сдача → полная автономия. 2. Расширение за рамки сайтов: игры, приложения, брендинг, SMM, оформление соцсетей. И новая ветка — TikTok/YouTube: анализ трендов, создание каналов, ведение, генерация видео, монетизация.

🚀 Продолжаем
Ты хочешь, чтобы Atlas стал автономной digital-студией, включая видео-контент. Но сейчас у нас нет ни одного рабочего пайплайна для видео. Давай честно: генерация видео — это отдельный сложный домен (не текст/картинки).
Мой план продолжения:
A. Довести web до денег (2-3 недели) - SEO-модуль, форма → Telegram, Auto-Deploy (GitHub Pages), QA Agent, портфолио-демо. - Продать первые 3-5 сайтов вручную, заработать $200-500, понять клиента.
B. Параллельно — видео-ветка (исследование) - Подключить бесплатные AI-видео (Hugging Face: CogVideo, AnimateDiff, или API типа Kling/Hailuo через free-tier). - Создать video_agent.py — генерация short-form видео (TikTok/Reels/Shorts) из текста + картинок. - Auto-uploader? Требует cookies/API TikTok/YouTube — риск бана, нестабильно.
C. Автономные продажи (после первых денег) - Browser Agent для парсинга лидов. - Telegram-бот для переписки. - Auto-invoice (ЮKassa/Stripe).
Что выбираешь прямо сейчас? 1. Вернуться к web — доделать SEO + Deploy + QA, чтобы продать первый сайт? 2. Исследовать видео — найти бесплатные AI-видео API и сделать тестовый ролик? 3. Начать авто-лиды — Browser Agent для Avito/Услуги?

💾 Текст для твоей памяти Kimi (если захочешь сохранить):
«Atlas — локальная digital-студия на Python/Ollama. Фокус: web-сайты через skills (HTML-шаблоны). Архитектура: Product Router → Executive → Brief → Image Generator → Developer → QA → Deploy. Есть motion_premium (GSAP), auto_skill_hunter, Model Router (7 облачных API + Ollama). GitHub: koghuhdlageneratorarf-eng/Atlas. Цель: автономный бизнес (лиды → продажи → продукт → оплата) + видео-контент TikTok/YouTube. Железо: GTX 1650 Ti 4GB.»
(Если хочешь, я запишу это в память через инструмент — скажи «сохрани в память».)

User:
Смотри, сейчас основная проблема, что мы с тобой условно мозг проекта, но ты никак не подключён напрямую к нему, а установленный локально qwen не очень хорош. Я считаю что главный приоритет сейчас это настроить системе мозги(то что мы уже обсуждали), связка из различных моделей. Чтобы она была подключена к проекту и могла уже напрямую отдавать задания для агентов - строителей проекта Atlas, как считаешь?

Kimi:
Согласен на все сто. Главная проблема — Atlas сейчас без «мозга». Локальный Qwen 3B/7B — это периферийная нервная система, а не мозг. Нужен Model Router, который:
Думает через облако (Gemini/Cerebras/Groq) — планирование, ТЗ, анализ.
Строит через локалку (Ollama 3B) — код, шаблоны, рутина.
Связывает агентов через Memory/context.json — чтобы они видели, что делали другие, без моего участия.
Тогда я становлюсь архитектором, а Atlas — автономным исполнителем.

🧠 Atlas Brain Setup — файлы для внедрения
Создаю 4 ключевых файла. Ты заменишь ими старые — и система получит мозги.
Что меняется

📦 Файлы для скачивания

User:
Скорее не совсем, у тебя постоянно заканчивается лимит контекста, поэтому приходится делать чекпоинты, так или иначе цепочка немного теряется, я думаю нужно использовать все эти топовые модели( облачные и локальные) чтобы они непосредственно видели проект изнутри, а также чтобы была устроена память проекта достаточно хорошо, возможно через скилл graphify

Kimi:
Ты абсолютно прав. Сейчас я — бутылочное горлышко. Каждый цикл: ты копируешь код в чат → я генерирую новый → ты вставляешь в PowerShell. Это не масштабируется. Atlas должен стать Self-Hosted Brain — системой, где облачные модели напрямую читают файлы проекта, помнят всю историю, и отдают задания агентам без моего посредничества.
Диагноз
Решение: Atlas Brain v2.0
Три слоя, которые нужно построить прямо сейчас:
1. Perception Layer — «Atlas видит себя»
Агент brain_indexer.py индексирует весь проект (все .py, .html, .md, .json) в векторную БД. При каждом вызове LLM релевантные файлы подставляются в промпт автоматически.
Стек: ChromaDB (локально, без сервера) + sentence-transformers (маленькая модель эмбеддингов, работает на CPU).
2. Memory Core — «Atlas помнит»
Не просто upgrade_suggestions.json, а полноценная память: - Episodic: что делали сегодня, какие баги, какие решения - Semantic: что такое motion_premium, какой skill для чего - Graph (то, что ты назвал graphify): кто какой агент, кто от кого зависит, какие файлы связаны
Стек: SQLite (структура) + ChromaDB (вектор) + NetworkX (граф зависимостей агентов → файлов → задач).
3. Cognition Layer — «Atlas думает сам»
llm_client.py превращается в Brain Router: - Gemini 2.5 Pro — архитектура, планирование, сложные решения - Cerebras Llama 4 — скоростные задачи (1M токенов/день) - Groq — резерв, когда Cerebras спит - OpenRouter free — авто-подбор, если всё упало - Ollama Qwen — только если интернет пропал
Ключевое: при вызове ask_llm() система автоматически прикладывает к промпту: - Релевантные файлы из индекса - Историю похожих задач из памяти - Текущий статус проекта из графа

Что значит «модели видят проект изнутри»
Сейчас Executive получает пустой промпт. После внедрения Brain он получает:
[SYSTEM] Ты — Executive Agent в Atlas.
[CONTEXT] Текущие файлы проекта:
  - Config/llm_client.py (Model Router, 7 провайдеров)
  - Agents/developer.py (универсальный, AOS-инжекция)
  - Skills/web/motion_premium/template.html (GSAP/Lenis)
[MEMORY] Вчера был баг: developer.py падал на BOM в skill.json. Фикс: utf-8-sig.
[GRAPH] Developer → зависит от → llm_client.py → зависит от → .env
[TASK] Сделай лендинг для кофейни...
Модель знает, с чем работает, и не предлагает «создать папку Config», потому что видит, что она уже есть.

Конкретный план (что кодим сейчас)
Шаг A. Создать Brain/ модуль
Atlas/
├── Brain/
│   ├── __init__.py
│   ├── indexer.py          # Индексация файлов в ChromaDB
│   ├── memory.py           # SQLite + Graph (NetworkX)
│   ├── context_builder.py  # Сборка контекста для LLM
│   └── observer.py         # Отслеживание изменений файлов
Шаг B. Обновить llm_client.py → brain_router.py
Перед каждым вызовом: context_builder.build(agent_name, task) → достаёт релевантные файлы и память
Передаёт это в Gemini/Cerebras вместе с задачей
Шаг C. Сделать autonomous_loop.py
Запускается ночью или по таймеру: 1. Self-Upgrade анализирует код через Gemini 2.5 Pro (с полным контекстом) 2. Результат пишется в Brain/memory.py 3. Если найден баг — Executive ставит задачу Developer на фикс 4. Утром ты видишь отчёт: «Исправлено 2 бага, добавлен 1 skill»

Про graphify
Ты упомянул graphify — это конкретный инструмент (GraphRAG от Microsoft), или ты имеешь в виду концепцию «графа знаний»?
Если конкретный инструмент — скажи, я адаптирую. Если концепция — я предлагаю свою лёгкую реализацию на NetworkX + SQLite, потому что GraphRAG требует Azure/OpenAI и тяжёл для твоего железа. Наш вариант: - Узлы: файлы, агенты, задачи, баги, skills - Рёбра: developer.py → использует → llm_client.py; баг #12 → в → developer.py; задача "кофейня" → создала → Projects/coffee/

Что делаем прямо сейчас
Я предлагаю не ждать, а за 20 минут построить скелет Brain. Нужно:
Создать Brain/indexer.py — он просканирует весь Atlas/ и построит векторный индекс + граф файлов
Создать Brain/memory.py — простая SQLite с таблицами: episodes, bugs, decisions, agent_states
Обновить llm_client.py — добавить auto_context=True, который перед вызовом LLM подтягивает релевантные файлы из индекса
После этого Executive сможет думать с полным пониманием проекта, а не вслепую.
Начинаем с Brain/indexer.py? Или хочешь сначала уточнить про graphify?

Kimi:
Отлично, теперь я вижу — Graphify это конкретный open-source проект (github.com/Graphify-Labs/graphify), который строит интерактивную карту знаний из кода. Это именно то, что нужно для Atlas — чтобы модели “видели” проект не как набор файлов, а как связанную структуру.
Давай посмотрю, что умеет Graphify, и адаптируем под Atlas.Теперь я вижу полную картину. Graphify — это open-source Python-инструмент, который:
Парсит код через tree-sitter (23 языка, полностью локально, код не уходит наружу)
Строит knowledge graph: graph.json + GRAPH_REPORT.md + интерактивный graph.html
Тегирует связи: EXTRACTED (точно из кода), INFERRED (догадка), AMBIGUOUS
Снижает потребление токенов на 71.5× — LLM ходит по графу вместо чтения всех файлов
Имеет MCP server — LLM может вызывать graphify query как инструмент
Это идеально для Atlas. Давай внедрим.

🧠 Atlas Brain v2.0 с Graphify
Архитектура
Atlas/
├── Brain/
│   ├── graphify_bridge.py    # Адаптер: запускает graphify, читает graph.json
│   ├── context_builder.py    # Собирает контекст для LLM из графа + памяти
│   ├── memory_graph.py       # Дополняет Graphify своими узлами (баги, решения)
│   └── observer.py           # Следит за изменениями файлов, перестраивает граф
├── Config/
│   └── llm_client.py         # + use_graph=True → авто-подгрузка контекста
└── graphify-out/             # Авто-создаётся при первом запуске
    ├── graph.json            # Весь проект как граф
    ├── GRAPH_REPORT.md       # "Вики" проекта для LLM
    └── graph.html            # Интерактивная визуализация
Как это работает
Раньше (сейчас):
Executive получает: "Сделай лендинг"
→ LLM не знает, что у Atlas уже есть motion_premium
→ Предлагает создать папку Config/ (уже есть)
→ Тратит токены на догадки
После Graphify:
Executive получает: "Сделай лендинг"
→ context_builder запрашивает у graphify_bridge:
   "Какие skills есть для web?"
→ graphify_bridge читает graph.json:
   Находит: Skills/web/motion_premium/skill.json → entry: template.html
            Skills/web/product_showcase/skill.json → entry: template.html
            Agents/developer.py → использует → Skills/
→ Формирует контекст:
   "Доступные skills: motion_premium (GSAP/Lenis), product_showcase..."
   "Developer агент находится в Agents/developer.py, зависит от Config/llm_client.py"
→ LLM получает этот контекст + задачу
→ Ответ точный, без галлюцинаций

📦 Установка и интеграция
Шаг 1. Установить Graphify
В PowerShell (в папке Atlas/):
pip install graphifyy
Шаг 2. Создать Brain/graphify_bridge.py
Этот файл — адаптер между Atlas и Graphify. Он запускает graphify, кэширует graph.json и отвечает на запросы агентов.
$code = @'
"""Graphify Bridge — адаптер между Atlas и Graphify knowledge graph."""
import json
import subprocess
import time
from pathlib import Path
from typing import List, Dict, Any, Optional

BASE_DIR = Path(__file__).parent.parent
GRAPHIFY_OUT = BASE_DIR / "graphify-out"
GRAPH_JSON = GRAPHIFY_OUT / "graph.json"
GRAPH_REPORT = GRAPHIFY_OUT / "GRAPH_REPORT.md"
CACHE_TS = BASE_DIR / "Brain" / ".graphify_cache"

class GraphifyBridge:
    """Управляет graphify: запуск, чтение, запросы."""
    
    def __init__(self):
        self._graph: Optional[Dict] = None
        self._report: Optional[str] = None
        self._last_modified: float = 0
    
    def _needs_rebuild(self) -> bool:
        """Проверяет, изменились ли файлы проекта с момента последнего графа."""
        if not GRAPH_JSON.exists():
            return True
        graph_mtime = GRAPH_JSON.stat().st_mtime
        # Ищем самый свежий файл в проекте (кроме graphify-out и .git)
        latest = 0
        for f in BASE_DIR.rglob("*"):
            if f.is_file() and ".git" not in str(f) and "graphify-out" not in str(f):
                try:
                    mtime = f.stat().st_mtime
                    if mtime > latest:
                        latest = mtime
                except:
                    pass
        return latest > graph_mtime
    
    def build(self, force: bool = False):
        """Запускает graphify . если нужно."""
        if not force and not self._needs_rebuild():
            print("[Graphify] Граф актуален, пропускаю.")
            return
        
        print("[Graphify] Перестраиваю knowledge graph...")
        GRAPHIFY_OUT.mkdir(exist_ok=True)
        
        # Запускаем graphify (только код, без документов/картинок — легко и быстро)
        result = subprocess.run(
            ["graphify", ".", "--output", str(GRAPHIFY_OUT), "--code-only"],
            cwd=str(BASE_DIR),
            capture_output=True,
            text=True
        )
        
        if result.returncode != 0:
            print(f"[!] Graphify ошибка: {result.stderr[:500]}")
            # Fallback: строим простой граф руками
            self._build_fallback()
        else:
            print("[Graphify] Граф построен.")
            self._load()
    
    def _build_fallback(self):
        """Если graphify не установлен — строим простой граф из файлов."""
        print("[Graphify] Fallback: ручная индексация...")
        nodes = []
        edges = []
        
        for f in BASE_DIR.rglob("*.py"):
            rel = str(f.relative_to(BASE_DIR)).replace("\\", "/")
            nodes.append({
                "id": rel,
                "type": "file",
                "label": f.name,
                "community": str(f.relative_to(BASE_DIR).parts[0]) if f.relative_to(BASE_DIR).parts else "root"
            })
        
        # Связи: импорты
        for node in nodes:
            fpath = BASE_DIR / node["id"]
            try:
                content = fpath.read_text(encoding="utf-8", errors="ignore")
                for line in content.split("\n"):
                    if line.strip().startswith("import ") or line.strip().startswith("from "):
                        # Простой парсинг импортов
                        parts = line.strip().split()
                        if len(parts) >= 2:
                            target = parts[1].split(".")[0] + ".py"
                            # Ищем файл
                            for n in nodes:
                                if n["label"] == target or n["label"].replace(".py", "") == target.replace(".py", ""):
                                    edges.append({
                                        "source": node["id"],
                                        "target": n["id"],
                                        "relation": "imports",
                                        "confidence": "EXTRACTED"
                                    })
            except:
                pass
        
        self._graph = {"nodes": nodes, "edges": edges}
        GRAPHIFY_OUT.mkdir(exist_ok=True)
        GRAPH_JSON.write_text(json.dumps(self._graph, indent=2, ensure_ascii=False), encoding="utf-8")
    
    def _load(self):
        """Загружает graph.json в память."""
        if GRAPH_JSON.exists():
            self._graph = json.loads(GRAPH_JSON.read_text(encoding="utf-8"))
        if GRAPH_REPORT.exists():
            self._report = GRAPH_REPORT.read_text(encoding="utf-8")
    
    def get_context(self, query: str, max_nodes: int = 15) -> str:
        """Возвращает релевантный контекст из графа для LLM."""
        if self._graph is None:
            self._load()
            if self._graph is None:
                return ""
        
        query_lower = query.lower()
        nodes = self._graph.get("nodes", [])
        edges = self._graph.get("edges", [])
        
        # Находим релевантные узлы по имени/типу
        relevant = []
        for node in nodes:
            score = 0
            label = node.get("label", "").lower()
            node_type = node.get("type", "").lower()
            community = node.get("community", "").lower()
            
            # Прямое совпадение
            if any(word in label for word in query_lower.split()):
                score += 10
            if any(word in community for word in query_lower.split()):
                score += 5
            # Ключевые слова
            if "skill" in query_lower and "skill" in label:
                score += 8
            if "agent" in query_lower and "agent" in label:
                score += 8
            if "developer" in query_lower and "developer" in label:
                score += 8
            
            if score > 0:
                relevant.append((score, node))
        
        # Сортируем по релевантности
        relevant.sort(key=lambda x: x[0], reverse=True)
        top_nodes = [n for _, n in relevant[:max_nodes]]
        
        # Находим соседей (1 hop) для топовых узлов
        top_ids = {n["id"] for n in top_nodes}
        neighbor_edges = []
        for edge in edges:
            if edge.get("source") in top_ids or edge.get("target") in top_ids:
                neighbor_edges.append(edge)
                # Добавляем соседние узлы
                for node in nodes:
                    if node["id"] == edge.get("source") or node["id"] == edge.get("target"):
                        if node not in top_nodes:
                            top_nodes.append(node)
        
        # Формируем текстовый контекст
        lines = ["=== ATLAS PROJECT STRUCTURE (from Graphify) ===", ""]
        
        # Группируем по community
        communities = {}
        for node in top_nodes:
            comm = node.get("community", "other")
            if comm not in communities:
                communities[comm] = []
            communities[comm].append(node)
        
        for comm, comm_nodes in sorted(communities.items()):
            lines.append(f"📁 {comm}/")
            for node in comm_nodes[:5]:  # макс 5 на папку
                lines.append(f"  • {node.get('label', node['id'])} ({node.get('type', 'file')})")
            if len(comm_nodes) > 5:
                lines.append(f"  ... и ещё {len(comm_nodes)-5}")
            lines.append("")
        
        # Связи
        if neighbor_edges:
            lines.append("=== КЛЮЧЕВЫЕ СВЯЗИ ===")
            for edge in neighbor_edges[:10]:
                lines.append(f"  {edge.get('source', '?')} --[{edge.get('relation', '?')}]--> {edge.get('target', '?')} ({edge.get('confidence', '')})")
        
        # Добавляем начало GRAPH_REPORT если есть
        if self._report:
            lines.append("")
            lines.append("=== PROJECT WIKI (GRAPH_REPORT) ===")
            lines.append(self._report[:1500])  # первые 1500 символов
            lines.append("...")
        
        return "\n".join(lines)
    
    def find_skill(self, skill_name: str) -> Optional[Dict]:
        """Находит skill по имени в графе."""
        if self._graph is None:
            self._load()
        for node in self._graph.get("nodes", []):
            if skill_name.lower() in node.get("label", "").lower():
                return node
        return None
    
    def get_file_neighbors(self, file_path: str) -> List[str]:
        """Возвращает соседей файла (что импортирует, что использует)."""
        if self._graph is None:
            self._load()
        neighbors = []
        for edge in self._graph.get("edges", []):
            if edge.get("source") == file_path:
                neighbors.append(f"{edge.get('relation')} -> {edge.get('target')}")
            elif edge.get("target") == file_path:
                neighbors.append(f"{edge.get('source')} -> {edge.get('relation')}")
        return neighbors

# Singleton
_bridge = GraphifyBridge()

def build_graph(force: bool = False):
    """Публичная функция для перестройки графа."""
    _bridge.build(force=force)

def get_context(query: str, max_nodes: int = 15) -> str:
    """Получить контекст для LLM."""
    return _bridge.get_context(query, max_nodes)

def find_skill(skill_name: str) -> Optional[Dict]:
    """Найти skill."""
    return _bridge.find_skill(skill_name)

def get_file_neighbors(file_path: str) -> List[str]:
    """Соседи файла."""
    return _bridge.get_file_neighbors(file_path)

if __name__ == "__main__":
    build_graph(force=True)
    print(get_context("какие skills для web", max_nodes=10))
'@

Set-Content -Path Brain/graphify_bridge.py -Value $code -Encoding UTF8
Шаг 3. Обновить Config/llm_client.py — авто-подгрузка контекста
Добавляем use_graph=True в ask_llm. Перед вызовом модели система сама достаёт релевантные файлы из графа.
$code = @'
"""Atlas Brain Router — LLM client with Graphify context + multi-provider fallback."""
import os
import sys
import json
import time
import requests
import yaml
from pathlib import Path
from dotenv import load_dotenv

# Graphify bridge
sys.path.insert(0, str(Path(__file__).parent.parent / "Brain"))
try:
    from graphify_bridge import get_context as graphify_context, build_graph
    GRAPHIFY_AVAILABLE = True
except ImportError:
    GRAPHIFY_AVAILABLE = False

# Load env
env_path = Path(__file__).parent / ".env"
if env_path.exists():
    load_dotenv(env_path)

# Config
CONFIG_PATH = Path(__file__).parent / "models.yaml"
with open(CONFIG_PATH, "r", encoding="utf-8") as f:
    MODELS_CONFIG = yaml.safe_load(f)

OLLAMA_URL = "http://localhost:11434/api/chat"
DEFAULT_MODEL = "qwen2.5-coder:3b"

# API keys
GEMINI_KEY = os.getenv("GEMINI_API_KEY", "")
CEREBRAS_KEY = os.getenv("CEREBRAS_API_KEY", "")
GROQ_KEY = os.getenv("GROQ_API_KEY", "")
OPENROUTER_KEY = os.getenv("OPENROUTER_API_KEY", "")
CLOUDFLARE_TOKEN = os.getenv("CLOUDFLARE_API_TOKEN", "")
HF_TOKEN = os.getenv("HUGGINGFACE_TOKEN", "")

# Provider endpoints
PROVIDERS = {
    "gemini": {
        "url": "https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-pro:generateContent",
        "key": GEMINI_KEY,
        "header": lambda k: {"x-goog-api-key": k},
        "payload": lambda msg: {"contents": [{"role": m["role"], "parts": [{"text": m["content"]}]} for m in msg], "generationConfig": {"temperature": 0.3}},
        "extract": lambda r: r["candidates"][0]["content"]["parts"][0]["text"]
    },
    "cerebras": {
        "url": "https://api.cerebras.ai/v1/chat/completions",
        "key": CEREBRAS_KEY,
        "header": lambda k: {"Authorization": f"Bearer {k}"},
        "payload": lambda msg: {"model": "llama-4-scout-17b-16e-instruct", "messages": msg, "temperature": 0.3},
        "extract": lambda r: r["choices"][0]["message"]["content"]
    },
    "groq": {
        "url": "https://api.groq.com/openai/v1/chat/completions",
        "key": GROQ_KEY,
        "header": lambda k: {"Authorization": f"Bearer {k}"},
        "payload": lambda msg: {"model": "llama-3.3-70b-versatile", "messages": msg, "temperature": 0.3},
        "extract": lambda r: r["choices"][0]["message"]["content"]
    },
    "openrouter": {
        "url": "https://openrouter.ai/api/v1/chat/completions",
        "key": OPENROUTER_KEY,
        "header": lambda k: {"Authorization": f"Bearer {k}", "HTTP-Referer": "https://atlas.local", "X-Title": "Atlas"},
        "payload": lambda msg: {"model": "openrouter/auto", "messages": msg, "temperature": 0.3},
        "extract": lambda r: r["choices"][0]["message"]["content"]
    },
    "cloudflare": {
        "url": f"https://api.cloudflare.com/client/v4/accounts/{os.getenv('CLOUDFLARE_ACCOUNT_ID','')}/ai/run/@cf/meta/llama-3.1-8b-instruct",
        "key": CLOUDFLARE_TOKEN,
        "header": lambda k: {"Authorization": f"Bearer {k}"},
        "payload": lambda msg: {"messages": msg},
        "extract": lambda r: r["result"]["response"]
    },
    "huggingface": {
        "url": "https://api-inference.huggingface.co/models/microsoft/Phi-3-mini-4k-instruct",
        "key": HF_TOKEN,
        "header": lambda k: {"Authorization": f"Bearer {k}"},
        "payload": lambda msg: {"inputs": msg[-1]["content"], "parameters": {"max_new_tokens": 1024}},
        "extract": lambda r: r[0]["generated_text"] if isinstance(r, list) else r.get("generated_text", "")
    }
}

def _call_provider(name: str, messages: list, timeout: int = 60) -> str:
    """Вызвать конкретного провайдера."""
    cfg = PROVIDERS[name]
    if not cfg["key"]:
        raise ValueError(f"No API key for {name}")
    
    headers = cfg["header"](cfg["key"])
    headers["Content-Type"] = "application/json"
    
    # Gemini uses GET params for key
    url = cfg["url"]
    if name == "gemini":
        url = f"{url}?key={cfg['key']}"
        headers = {"Content-Type": "application/json"}
    
    response = requests.post(url, headers=headers, json=cfg["payload"](messages), timeout=timeout)
    response.raise_for_status()
    return cfg["extract"](response.json())

def _call_ollama(messages: list, model: str = DEFAULT_MODEL, timeout: int = 120) -> str:
    """Fallback на локальную Ollama."""
    response = requests.post(
        OLLAMA_URL,
        json={"model": model, "messages": messages, "stream": False},
        timeout=timeout
    )
    response.raise_for_status()
    return response.json()["message"]["content"]

def ask_llm(messages: list, agent: str = "developer", use_graph: bool = True, timeout: int = 120) -> str:
    """
    Умный роутер с авто-контекстом из Graphify.
    
    Args:
        messages: список сообщений
        agent: "executive", "brief", "developer", "self_upgrade"
        use_graph: подгружать ли контекст из Graphify
    """
    # 1. Определяем приоритет провайдеров для агента
    priorities = MODELS_CONFIG.get("agents", {}).get(agent, ["ollama"])
    
    # 2. Подгружаем контекст из Graphify
    if use_graph and GRAPHIFY_AVAILABLE:
        # Извлекаем задачу из последнего сообщения
        task = messages[-1].get("content", "") if messages else ""
        context = graphify_context(task, max_nodes=12)
        if context:
            # Вставляем контекст как system-сообщение перед задачей
            context_msg = {
                "role": "system",
                "content": f"=== КОНТЕКСТ ПРОЕКТА ATLAS ===\n{context}\n=== КОНЕЦ КОНТЕКСТА ===\n\nИспользуй эту информацию при ответе. Не предлагай создавать файлы/папки, которые уже существуют."
            }
            # Вставляем после system, перед user
            new_messages = []
            has_system = False
            for m in messages:
                if m["role"] == "system" and not has_system:
                    new_messages.append(m)
                    new_messages.append(context_msg)
                    has_system = True
                else:
                    new_messages.append(m)
            if not has_system:
                new_messages = [context_msg] + messages
            messages = new_messages
    
    # 3. Пробуем провайдеры по очереди
    last_error = None
    for provider in priorities:
        if provider == "ollama":
            try:
                model = MODELS_CONFIG.get("ollama_models", {}).get(agent, DEFAULT_MODEL)
                print(f"[Brain] {agent} → Ollama ({model})")
                return _call_ollama(messages, model, timeout)
            except Exception as e:
                last_error = e
                print(f"[!] Ollama failed: {e}")
                continue
        
        if provider in PROVIDERS:
            try:
                print(f"[Brain] {agent} → {provider.upper()}")
                return _call_provider(provider, messages, timeout)
            except Exception as e:
                last_error = e
                print(f"[!] {provider} failed: {e}")
                continue
    
    # 4. Все упали
    raise RuntimeError(f"All providers failed. Last error: {last_error}")

def diagnose():
    """Проверяет всех провайдеров."""
    print("=" * 50)
    print("ATLAS BRAIN DIAGNOSTIC")
    print("=" * 50)
    
    # Graphify
    print(f"\n[Graphify] Available: {GRAPHIFY_AVAILABLE}")
    if GRAPHIFY_AVAILABLE:
        from graphify_bridge import build_graph
        build_graph()
        ctx = graphify_context("skills web", max_nodes=5)
        print(f"Context sample: {ctx[:200]}...")
    
    # Providers
    test_msg = [{"role": "user", "content": "Say 'OK' only"}]
    for name, cfg in PROVIDERS.items():
        status = "✅" if cfg["key"] else "❌ NO KEY"
        print(f"\n[{name.upper()}] {status}")
        if cfg["key"]:
            try:
                start = time.time()
                result = _call_provider(name, test_msg, timeout=30)
                elapsed = time.time() - start
                print(f"  Response: {result[:50]}... ({elapsed:.1f}s)")
            except Exception as e:
                print(f"  Error: {e}")
    
    # Ollama
    print("\n[OLLAMA]")
    try:
        r = requests.get("http://localhost:11434/api/tags", timeout=5)
        models = [m["name"] for m in r.json().get("models", [])]
        print(f"  Models: {models}")
    except Exception as e:
        print(f"  Error: {e}")

if __name__ == "__main__":
    import argparse
    parser = argparse.ArgumentParser()
    parser.add_argument("--diagnose", action="store_true")
    parser.add_argument("--test", choices=list(PROVIDERS.keys()) + ["ollama"])
    args = parser.parse_args()
    
    if args.diagnose:
        diagnose()
    elif args.test:
        msg = [{"role": "user", "content": "Привет, это тест Atlas Brain. Ответь кратко."}]
        if args.test == "ollama":
            print(_call_ollama(msg))
        else:
            print(_call_provider(args.test, msg))
    else:
        # Quick test with graph context
        result = ask_llm(
            [{"role": "user", "content": "Какие skills доступны для создания сайтов?"}],
            agent="executive"
        )
        print(result)
'@

Set-Content -Path Config/llm_client.py -Value $code -Encoding UTF8
Шаг 4. Обновить models.yaml (приоритеты с Graphify-осведомлённостью)
$yaml = @'
ollama_models:
  executive: "qwen2.5-coder:7b"
  brief: "qwen2.5-coder:7b"
  developer: "qwen2.5-coder:3b"
  self_upgrade: "qwen2.5-coder:7b"
  branding: "qwen2.5-coder:7b"
  media: "qwen2.5-coder:3b"

agents:
  executive:
    - gemini
    - cerebras
    - groq
    - ollama
  brief:
    - gemini
    - cerebras
    - groq
    - ollama
  developer:
    - ollama
    - openrouter
    - cloudflare
  self_upgrade:
    - gemini
    - cerebras
    - ollama
  branding:
    - gemini
    - cerebras
    - ollama
  media:
    - ollama
    - openrouter
'@

Set-Content -Path Config/models.yaml -Value $yaml -Encoding UTF8
Шаг 5. Создать Brain/memory_graph.py — память поверх Graphify
Graphify знает структуру кода. Но Atlas также нуждается в истории: какие баги были, какие решения приняты, что работает, что нет.
$code = @'
"""Memory Graph — эпизодическая память Atlas поверх Graphify."""
import json
import sqlite3
from datetime import datetime
from pathlib import Path

DB_PATH = Path(__file__).parent.parent / "Memory" / "brain_memory.db"

def _init_db():
    """Инициализация SQLite."""
    DB_PATH.parent.mkdir(parents=True, exist_ok=True)
    conn = sqlite3.connect(str(DB_PATH))
    c = conn.cursor()
    c.execute('''
        CREATE TABLE IF NOT EXISTS episodes (
            id INTEGER PRIMARY KEY AUTOINCREMENT,
            timestamp TEXT,
            agent TEXT,
            task TEXT,
            action TEXT,
            result TEXT,
            files_changed TEXT,
            status TEXT
        )
    ''')
    c.execute('''
        CREATE TABLE IF NOT EXISTS bugs (
            id INTEGER PRIMARY KEY AUTOINCREMENT,
            timestamp TEXT,
            file TEXT,
            description TEXT,
            fix TEXT,
            fixed_at TEXT
        )
    ''')
    c.execute('''
        CREATE TABLE IF NOT EXISTS decisions (
            id INTEGER PRIMARY KEY AUTOINCREMENT,
            timestamp TEXT,
            context TEXT,
            decision TEXT,
            reason TEXT
        )
    ''')
    conn.commit()
    conn.close()

def log_episode(agent: str, task: str, action: str, result: str, files_changed: list = None, status: str = "success"):
    """Записать событие."""
    _init_db()
    conn = sqlite3.connect(str(DB_PATH))
    c = conn.cursor()
    c.execute(
        "INSERT INTO episodes (timestamp, agent, task, action, result, files_changed, status) VALUES (?, ?, ?, ?, ?, ?, ?)",
        (datetime.now().isoformat(), agent, task, action, result, json.dumps(files_changed or []), status)
    )
    conn.commit()
    conn.close()

def log_bug(file: str, description: str, fix: str = ""):
    """Записать баг."""
    _init_db()
    conn = sqlite3.connect(str(DB_PATH))
    c = conn.cursor()
    c.execute(
        "INSERT INTO bugs (timestamp, file, description, fix, fixed_at) VALUES (?, ?, ?, ?, ?)",
        (datetime.now().isoformat(), file, description, fix, datetime.now().isoformat() if fix else None)
    )
    conn.commit()
    conn.close()

def log_decision(context: str, decision: str, reason: str):
    """Записать архитектурное решение."""
    _init_db()
    conn = sqlite3.connect(str(DB_PATH))
    c = conn.cursor()
    c.execute(
        "INSERT INTO decisions (timestamp, context, decision, reason) VALUES (?, ?, ?, ?)",
        (datetime.now().isoformat(), context, decision, reason)
    )
    conn.commit()
    conn.close()

def get_recent_context(agent: str = None, limit: int = 5) -> str:
    """Получить недавнюю историю для контекста LLM."""
    if not DB_PATH.exists():
        return ""
    conn = sqlite3.connect(str(DB_PATH))
    c = conn.cursor()
    
    if agent:
        c.execute("SELECT timestamp, task, action, result, status FROM episodes WHERE agent = ? ORDER BY id DESC LIMIT ?", (agent, limit))
    else:
        c.execute("SELECT timestamp, task, action, result, status FROM episodes ORDER BY id DESC LIMIT ?", (limit,))
    
    rows = c.fetchall()
    conn.close()
    
    if not rows:
        return ""
    
    lines = ["=== НЕДАВНЯЯ ИСТОРИЯ ==="]
    for ts, task, action, result, status in rows:
        lines.append(f"[{ts[:10]}] {action}: {result[:100]} ({status})")
    
    # Добавляем нерешённые баги
    conn = sqlite3.connect(str(DB_PATH))
    c = conn.cursor()
    c.execute("SELECT file, description FROM bugs WHERE fixed_at IS NULL LIMIT 3")
    bugs = c.fetchall()
    conn.close()
    
    if bugs:
        lines.append("\n=== АКТИВНЫЕ БАГИ ===")
        for file, desc in bugs:
            lines.append(f"[!] {file}: {desc[:100]}")
    
    return "\n".join(lines)

if __name__ == "__main__":
    # Тест
    log_episode("developer", "лендинг кофейни", "создание", "успех", ["Projects/coffee/index.html"])
    log_bug("Agents/developer.py", "BOM в skill.json", "utf-8-sig")
    print(get_recent_context("developer"))
'@

Set-Content -Path Brain/memory_graph.py -Value $code -Encoding UTF8
Шаг 6. Обновить main.py — интеграция Brain
$code = @'
"""Atlas Digital Studio — main orchestrator with Brain integration."""
import sys
import subprocess
import json
from pathlib import Path

# Инициализация Brain при старте
sys.path.insert(0, str(Path(__file__).parent / "Brain"))
try:
    from graphify_bridge import build_graph
    from memory_graph import log_episode
    BRAIN_READY = True
    # Строим/обновляем граф при старте
    build_graph()
except Exception as e:
    print(f"[!] Brain init error: {e}")
    BRAIN_READY = False

def run_subprocess(script_path, args, agent_name="unknown"):
    result = subprocess.run(
        [sys.executable, script_path] + args,
        capture_output=True, text=False
    )
    stdout = result.stdout.decode("utf-8", errors="replace") if result.stdout else ""
    stderr = result.stderr.decode("utf-8", errors="replace") if result.stderr else ""
    print(stdout)
    if stderr:
        print(stderr)
    
    # Логируем в Brain
    if BRAIN_READY:
        log_episode(
            agent=agent_name,
            task=" ".join(args[:2]) if args else "unknown",
            action=f"run {Path(script_path).name}",
            result=stdout[:500],
            status="success" if result.returncode == 0 else "error"
        )
    
    return result.returncode == 0

def main():
    if len(sys.argv) < 2:
        print("Atlas Digital Studio with Brain")
        print('Usage: python main.py "task" [skill_name]')
        sys.exit(1)

    task = sys.argv[1]
    skill_name = sys.argv[2] if len(sys.argv) > 2 else None
    
    # Определяем тип продукта
    task_lower = task.lower()
    if any(w in task_lower for w in ["brend", "brand", "logotip", "logo"]):
        product_type = "branding"
    elif any(w in task_lower for w in ["foto", "photo", "fotosessiya", "avatar"]):
        product_type = "media"
    elif any(w in task_lower for w in ["igra", "game", "play"]):
        product_type = "game"
    else:
        product_type = "web"

    words = task.replace('"', '').replace("'", "").split()[:3]
    project_name = "_".join(words).lower() + "_project"
    project_dir = f"./Projects/{project_name}"

    print("=" * 60)
    print("ATLAS DIGITAL STUDIO")
    if BRAIN_READY:
        print("[Brain] Graphify + Memory активны")
    print("=" * 60)
    print(f"\nTask: {task}")
    print(f"Type: {product_type}\n")

    if product_type == "branding":
        print("--- Branding Agent ---")
        run_subprocess("Agents/branding_agent.py", [task, project_dir], "branding")
    elif product_type == "media":
        print("--- Media Agent ---")
        run_subprocess("Agents/media_agent.py", [task, project_dir], "media")
    else:
        # Web pipeline
        print("--- Executive ---")
        run_subprocess("Agents/executive.py", [task], "executive")
        
        print("\n--- Brief Agent ---")
        run_subprocess("Agents/brief.py", [task, project_dir], "brief")
        
        print("\n--- Image Generator ---")
        tasks_file = Path(project_dir) / "image_tasks.json"
        if tasks_file.exists():
            tasks = json.loads(tasks_file.read_text(encoding="utf-8"))
            if tasks:
                run_subprocess("Agents/image_generator.py", [json.dumps(tasks), project_dir], "image_generator")
        
        print("\n--- Web Developer ---")
        skill = skill_name or "product_showcase"
        run_subprocess("Agents/developer.py", [task, project_dir, skill], "developer")

    print("\n" + "=" * 60)
    print("✅ ATLAS COMPLETE")
    print("=" * 60)
    print(f"\nProject: {Path(project_dir).absolute()}")

if __name__ == "__main__":
    main()
'@

Set-Content -Path main.py -Value $code -Encoding UTF8

🚀 Что получится
После запуска python main.py "...":
Brain инициализируется — graphify сканирует весь Atlas/ и строит graph.json
Executive вызывается — но теперь в его промпт автоматически подгружается:
Список всех skills (с путями)
Структура Agents/
Недавние баги из memory_graph.db
Executive знает, что motion_premium уже существует, и предложит его
Developer знает, что developer.py зависит от llm_client.py, и не сломает связи

⚡ Быстрый старт (когда будешь дома)
cd C:\Users\diman\Desktop\AI\Atlas

# 1. Установить Graphify
pip install graphifyy

# 2. Создать папку Brain
mkdir Brain

# 3. Вставить все 5 блоков кода выше (по очереди в PowerShell)

# 4. Проверить Brain
python Config\llm_client.py --diagnose

# 5. Запустить Atlas с умным контекстом
python main.py "Сделай лендинг для кофейни"
Теперь Atlas имеет мозг, который: - Видит себя изнутри (Graphify) - Помнит историю (SQLite) - Думает через облачные модели (Gemini/Cerebras/Groq) - Не забывает, что было вчера
Начинаем с этого? Или хочешь, чтобы я ещё добавил MCP server — чтобы внешние ассистенты (Claude Code, Cursor) тоже могли “ходить” по графу Atlas?

User:
Так, давай будем делать по шагам, и ты дожидайся всегда когда я закончу шаг

Kimi:
Понял. Делаем строго по шагам. Я дам только один шаг, жду твоего «готово», и только потом следующий.

Шаг 1: Установить Graphify и создать папку Brain
Выполни в PowerShell (в папке Atlas):
cd C:\Users\diman\Desktop\AI\Atlas
mkdir Brain
pip install graphifyy
Что проверить после выполнения: - Папка Brain/ создана - graphifyy установился без ошибок (должен написать Successfully installed graphifyy)
Напиши: готово + весь вывод из терминала. Только тогда Шаг 2.

User:
PS C:> cd C: >> mkdir Brain >> pip install graphifyy
Каталог: C:\Users\diman\Desktop\AI\Atlas
PS C:>

Kimi:
pip не в PATH. Исправляем — используем python -m pip:
python -m pip install graphifyy
Напиши: готово + вывод.

Kimi:
Шаг 2: Создать Brain/graphify_bridge.py
Выполни в PowerShell (в папке Atlas):
$code = @'
"""Graphify Bridge — адаптер между Atlas и Graphify knowledge graph."""
import json
import subprocess
import time
from pathlib import Path
from typing import List, Dict, Any, Optional

BASE_DIR = Path(__file__).parent.parent
GRAPHIFY_OUT = BASE_DIR / "graphify-out"
GRAPH_JSON = GRAPHIFY_OUT / "graph.json"
GRAPH_REPORT = GRAPHIFY_OUT / "GRAPH_REPORT.md"

class GraphifyBridge:
    """Управляет graphify: запуск, чтение, запросы."""
    
    def __init__(self):
        self._graph: Optional[Dict] = None
        self._report: Optional[str] = None
    
    def _needs_rebuild(self) -> bool:
        """Проверяет, изменились ли файлы проекта с момента последнего графа."""
        if not GRAPH_JSON.exists():
            return True
        graph_mtime = GRAPH_JSON.stat().st_mtime
        latest = 0
        for f in BASE_DIR.rglob("*"):
            if f.is_file() and ".git" not in str(f) and "graphify-out" not in str(f):
                try:
                    mtime = f.stat().st_mtime
                    if mtime > latest:
                        latest = mtime
                except:
                    pass
        return latest > graph_mtime
    
    def build(self, force: bool = False):
        """Запускает graphify если нужно."""
        if not force and not self._needs_rebuild():
            print("[Graphify] Граф актуален, пропускаю.")
            return
        
        print("[Graphify] Перестраиваю knowledge graph...")
        GRAPHIFY_OUT.mkdir(exist_ok=True)
        
        result = subprocess.run(
            ["graphify", ".", "--output", str(GRAPHIFY_OUT), "--code-only"],
            cwd=str(BASE_DIR),
            capture_output=True,
            text=True
        )
        
        if result.returncode != 0:
            print(f"[!] Graphify ошибка: {result.stderr[:500]}")
            self._build_fallback()
        else:
            print("[Graphify] Граф построен.")
            self._load()
    
    def _build_fallback(self):
        """Если graphify не работает — ручная индексация."""
        print("[Graphify] Fallback: ручная индексация...")
        nodes = []
        edges = []
        
        for f in BASE_DIR.rglob("*.py"):
            rel = str(f.relative_to(BASE_DIR)).replace("\\", "/")
            parts = f.relative_to(BASE_DIR).parts
            nodes.append({
                "id": rel,
                "type": "file",
                "label": f.name,
                "community": parts[0] if parts else "root"
            })
        
        for node in nodes:
            fpath = BASE_DIR / node["id"]
            try:
                content = fpath.read_text(encoding="utf-8", errors="ignore")
                for line in content.split("\n"):
                    line = line.strip()
                    if line.startswith("import ") or line.startswith("from "):
                        parts = line.split()
                        if len(parts) >= 2:
                            target = parts[1].split(".")[0]
                            if not target.startswith(("os", "sys", "json", "time", "pathlib", "typing")):
                                for n in nodes:
                                    if target in n["label"].replace(".py", ""):
                                        edges.append({
                                            "source": node["id"],
                                            "target": n["id"],
                                            "relation": "imports",
                                            "confidence": "EXTRACTED"
                                        })
            except:
                pass
        
        self._graph = {"nodes": nodes, "edges": edges}
        GRAPHIFY_OUT.mkdir(exist_ok=True)
        GRAPH_JSON.write_text(json.dumps(self._graph, indent=2, ensure_ascii=False), encoding="utf-8")
    
    def _load(self):
        """Загружает graph.json."""
        if GRAPH_JSON.exists():
            self._graph = json.loads(GRAPH_JSON.read_text(encoding="utf-8"))
        if GRAPH_REPORT.exists():
            self._report = GRAPH_REPORT.read_text(encoding="utf-8")
    
    def get_context(self, query: str, max_nodes: int = 15) -> str:
        """Возвращает релевантный контекст из графа для LLM."""
        if self._graph is None:
            self._load()
            if self._graph is None:
                return ""
        
        query_lower = query.lower()
        nodes = self._graph.get("nodes", [])
        edges = self._graph.get("edges", [])
        
        relevant = []
        for node in nodes:
            score = 0
            label = node.get("label", "").lower()
            community = node.get("community", "").lower()
            
            if any(word in label for word in query_lower.split()):
                score += 10
            if any(word in community for word in query_lower.split()):
                score += 5
            if "skill" in query_lower and "skill" in label:
                score += 8
            if "agent" in query_lower and "agent" in label:
                score += 8
            if "developer" in query_lower and "developer" in label:
                score += 8
            
            if score > 0:
                relevant.append((score, node))
        
        relevant.sort(key=lambda x: x[0], reverse=True)
        top_nodes = [n for _, n in relevant[:max_nodes]]
        
        top_ids = {n["id"] for n in top_nodes}
        neighbor_edges = []
        for edge in edges:
            if edge.get("source") in top_ids or edge.get("target") in top_ids:
                neighbor_edges.append(edge)
                for node in nodes:
                    if node["id"] == edge.get("source") or node["id"] == edge.get("target"):
                        if node not in top_nodes:
                            top_nodes.append(node)
        
        lines = ["=== ATLAS PROJECT STRUCTURE (from Graphify) ===", ""]
        
        communities = {}
        for node in top_nodes:
            comm = node.get("community", "other")
            if comm not in communities:
                communities[comm] = []
            communities[comm].append(node)
        
        for comm, comm_nodes in sorted(communities.items()):
            lines.append(f"📁 {comm}/")
            for node in comm_nodes[:5]:
                lines.append(f"  • {node.get('label', node['id'])} ({node.get('type', 'file')})")
            if len(comm_nodes) > 5:
                lines.append(f"  ... и ещё {len(comm_nodes)-5}")
            lines.append("")
        
        if neighbor_edges:
            lines.append("=== КЛЮЧЕВЫЕ СВЯЗИ ===")
            for edge in neighbor_edges[:10]:
                lines.append(f"  {edge.get('source', '?')} --[{edge.get('relation', '?')}]--> {edge.get('target', '?')}")
        
        if self._report:
            lines.append("")
            lines.append("=== PROJECT WIKI ===")
            lines.append(self._report[:1500])
            lines.append("...")
        
        return "\n".join(lines)
    
    def find_skill(self, skill_name: str) -> Optional[Dict]:
        if self._graph is None:
            self._load()
        for node in self._graph.get("nodes", []):
            if skill_name.lower() in node.get("label", "").lower():
                return node
        return None
    
    def get_file_neighbors(self, file_path: str) -> List[str]:
        if self._graph is None:
            self._load()
        neighbors = []
        for edge in self._graph.get("edges", []):
            if edge.get("source") == file_path:
                neighbors.append(f"{edge.get('relation')} -> {edge.get('target')}")
            elif edge.get("target") == file_path:
                neighbors.append(f"{edge.get('source')} -> {edge.get('relation')}")
        return neighbors

_bridge = GraphifyBridge()

def build_graph(force: bool = False):
    _bridge.build(force=force)

def get_context(query: str, max_nodes: int = 15) -> str:
    return _bridge.get_context(query, max_nodes)

def find_skill(skill_name: str) -> Optional[Dict]:
    return _bridge.find_skill(skill_name)

def get_file_neighbors(file_path: str) -> List[str]:
    return _bridge.get_file_neighbors(file_path)

if __name__ == "__main__":
    build_graph(force=True)
    print(get_context("skills web", max_nodes=10))
'@

Set-Content -Path Brain/graphify_bridge.py -Value $code -Encoding UTF8
Напиши: готово + вывод.

User:
Готово

Kimi:
Шаг 3: Обновить Config/llm_client.py — авто-подгрузка контекста из Graphify
Выполни в PowerShell (в папке Atlas):
$code = @'
"""Atlas Brain Router — LLM client with Graphify context + Ollama fallback."""
import os
import sys
import json
import requests
from pathlib import Path

# Graphify bridge
sys.path.insert(0, str(Path(__file__).parent.parent / "Brain"))
try:
    from graphify_bridge import get_context as graphify_context
    GRAPHIFY_AVAILABLE = True
except ImportError:
    GRAPHIFY_AVAILABLE = False

OLLAMA_URL = "http://localhost:11434/api/chat"
DEFAULT_MODEL = "qwen2.5-coder:3b"

def _call_ollama(messages: list, model: str = DEFAULT_MODEL, timeout: int = 120) -> str:
    """Fallback на локальную Ollama."""
    response = requests.post(
        OLLAMA_URL,
        json={"model": model, "messages": messages, "stream": False},
        timeout=timeout
    )
    response.raise_for_status()
    return response.json()["message"]["content"]

def ask_llm(messages: list, model: str = None, agent: str = "developer", use_graph: bool = True, timeout: int = 120) -> str:
    """
    Умный роутер с авто-контекстом из Graphify.
    
    Args:
        messages: список сообщений
        model: модель Ollama (если None — DEFAULT_MODEL)
        agent: "executive", "brief", "developer", "self_upgrade"
        use_graph: подгружать ли контекст из Graphify
    """
    use_model = model or DEFAULT_MODEL
    
    # 1. Подгружаем контекст из Graphify
    if use_graph and GRAPHIFY_AVAILABLE:
        task = messages[-1].get("content", "") if messages else ""
        context = graphify_context(task, max_nodes=12)
        if context:
            context_msg = {
                "role": "system",
                "content": f"=== КОНТЕКСТ ПРОЕКТА ATLAS ===\n{context}\n=== КОНЕЦ КОНТЕКСТА ===\n\nИспользуй эту информацию. Не предлагай создавать файлы/папки, которые уже существуют."
            }
            # Вставляем после первого system-сообщения или в начало
            new_messages = []
            has_system = False
            for m in messages:
                if m["role"] == "system" and not has_system:
                    new_messages.append(m)
                    new_messages.append(context_msg)
                    has_system = True
                else:
                    new_messages.append(m)
            if not has_system:
                new_messages = [context_msg] + messages
            messages = new_messages
    
    # 2. Вызываем Ollama (пока только локальная модель, облачные — на следующем шаге)
    print(f"[Brain] {agent} → Ollama ({use_model})")
    return _call_ollama(messages, use_model, timeout)

def diagnose():
    """Проверяет Brain."""
    print("=" * 50)
    print("ATLAS BRAIN DIAGNOSTIC")
    print("=" * 50)
    
    print(f"\n[Graphify] Available: {GRAPHIFY_AVAILABLE}")
    if GRAPHIFY_AVAILABLE:
        from graphify_bridge import build_graph
        build_graph()
        ctx = graphify_context("skills web", max_nodes=5)
        print(f"Context sample:\n{ctx[:500]}...")
    
    print("\n[OLLAMA]")
    try:
        r = requests.get("http://localhost:11434/api/tags", timeout=5)
        models = [m["name"] for m in r.json().get("models", [])]
        print(f"  Models: {models}")
    except Exception as e:
        print(f"  Error: {e}")

if __name__ == "__main__":
    import argparse
    parser = argparse.ArgumentParser()
    parser.add_argument("--diagnose", action="store_true")
    args = parser.parse_args()
    
    if args.diagnose:
        diagnose()
    else:
        # Quick test
        result = ask_llm(
            [{"role": "user", "content": "Какие skills доступны для создания сайтов?"}],
            agent="executive"
        )
        print(result)
'@

Set-Content -Path Config/llm_client.py -Value $code -Encoding UTF8
Напиши: готово + вывод.

User:
готово

Kimi:
Шаг 4: Создать Brain/memory_graph.py — эпизодическая память Atlas
Выполни в PowerShell (в папке Atlas):
$code = @'
"""Memory Graph — эпизодическая память Atlas поверх Graphify."""
import json
import sqlite3
from datetime import datetime
from pathlib import Path

DB_PATH = Path(__file__).parent.parent / "Memory" / "brain_memory.db"

def _init_db():
    """Инициализация SQLite."""
    DB_PATH.parent.mkdir(parents=True, exist_ok=True)
    conn = sqlite3.connect(str(DB_PATH))
    c = conn.cursor()
    c.execute('''
        CREATE TABLE IF NOT EXISTS episodes (
            id INTEGER PRIMARY KEY AUTOINCREMENT,
            timestamp TEXT,
            agent TEXT,
            task TEXT,
            action TEXT,
            result TEXT,
            files_changed TEXT,
            status TEXT
        )
    ''')
    c.execute('''
        CREATE TABLE IF NOT EXISTS bugs (
            id INTEGER PRIMARY KEY AUTOINCREMENT,
            timestamp TEXT,
            file TEXT,
            description TEXT,
            fix TEXT,
            fixed_at TEXT
        )
    ''')
    c.execute('''
        CREATE TABLE IF NOT EXISTS decisions (
            id INTEGER PRIMARY KEY AUTOINCREMENT,
            timestamp TEXT,
            context TEXT,
            decision TEXT,
            reason TEXT
        )
    ''')
    conn.commit()
    conn.close()

def log_episode(agent: str, task: str, action: str, result: str, files_changed: list = None, status: str = "success"):
    """Записать событие."""
    _init_db()
    conn = sqlite3.connect(str(DB_PATH))
    c = conn.cursor()
    c.execute(
        "INSERT INTO episodes (timestamp, agent, task, action, result, files_changed, status) VALUES (?, ?, ?, ?, ?, ?, ?)",
        (datetime.now().isoformat(), agent, task, action, result, json.dumps(files_changed or []), status)
    )
    conn.commit()
    conn.close()

def log_bug(file: str, description: str, fix: str = ""):
    """Записать баг."""
    _init_db()
    conn = sqlite3.connect(str(DB_PATH))
    c = conn.cursor()
    c.execute(
        "INSERT INTO bugs (timestamp, file, description, fix, fixed_at) VALUES (?, ?, ?, ?, ?)",
        (datetime.now().isoformat(), file, description, fix, datetime.now().isoformat() if fix else None)
    )
    conn.commit()
    conn.close()

def log_decision(context: str, decision: str, reason: str):
    """Записать архитектурное решение."""
    _init_db()
    conn = sqlite3.connect(str(DB_PATH))
    c = conn.cursor()
    c.execute(
        "INSERT INTO decisions (timestamp, context, decision, reason) VALUES (?, ?, ?, ?)",
        (datetime.now().isoformat(), context, decision, reason)
    )
    conn.commit()
    conn.close()

def get_recent_context(agent: str = None, limit: int = 5) -> str:
    """Получить недавнюю историю для контекста LLM."""
    if not DB_PATH.exists():
        return ""
    conn = sqlite3.connect(str(DB_PATH))
    c = conn.cursor()
    
    if agent:
        c.execute("SELECT timestamp, task, action, result, status FROM episodes WHERE agent = ? ORDER BY id DESC LIMIT ?", (agent, limit))
    else:
        c.execute("SELECT timestamp, task, action, result, status FROM episodes ORDER BY id DESC LIMIT ?", (limit,))
    
    rows = c.fetchall()
    conn.close()
    
    if not rows:
        return ""
    
    lines = ["=== НЕДАВНЯЯ ИСТОРИЯ ==="]
    for ts, task, action, result, status in rows:
        lines.append(f"[{ts[:10]}] {action}: {result[:100]} ({status})")
    
    # Нерешённые баги
    conn = sqlite3.connect(str(DB_PATH))
    c = conn.cursor()
    c.execute("SELECT file, description FROM bugs WHERE fixed_at IS NULL LIMIT 3")
    bugs = c.fetchall()
    conn.close()
    
    if bugs:
        lines.append("\n=== АКТИВНЫЕ БАГИ ===")
        for file, desc in bugs:
            lines.append(f"[!] {file}: {desc[:100]}")
    
    return "\n".join(lines)

if __name__ == "__main__":
    # Тест
    log_episode("developer", "лендинг кофейни", "создание", "успех", ["Projects/coffee/index.html"])
    log_bug("Agents/developer.py", "BOM в skill.json", "utf-8-sig")
    print(get_recent_context("developer"))
'@

Set-Content -Path Brain/memory_graph.py -Value $code -Encoding UTF8
Напиши: готово + вывод.

User:
готово

Kimi:
Шаг 5: Обновить main.py — интеграция Brain
Выполни в PowerShell (в папке Atlas):
$code = @'
"""Atlas Digital Studio — main orchestrator with Brain integration."""
import sys
import subprocess
import json
from pathlib import Path

# Инициализация Brain при старте
sys.path.insert(0, str(Path(__file__).parent / "Brain"))
try:
    from graphify_bridge import build_graph
    from memory_graph import log_episode
    BRAIN_READY = True
    build_graph()
except Exception as e:
    print(f"[!] Brain init error: {e}")
    BRAIN_READY = False

def run_subprocess(script_path, args, agent_name="unknown"):
    result = subprocess.run(
        [sys.executable, script_path] + args,
        capture_output=True, text=False
    )
    stdout = result.stdout.decode("utf-8", errors="replace") if result.stdout else ""
    stderr = result.stderr.decode("utf-8", errors="replace") if result.stderr else ""
    print(stdout)
    if stderr:
        print(stderr)
    
    if BRAIN_READY:
        log_episode(
            agent=agent_name,
            task=" ".join(args[:2]) if args else "unknown",
            action=f"run {Path(script_path).name}",
            result=stdout[:500],
            status="success" if result.returncode == 0 else "error"
        )
    
    return result.returncode == 0

def main():
    if len(sys.argv) < 2:
        print("Atlas Digital Studio with Brain")
        print('Usage: python main.py "task" [skill_name]')
        sys.exit(1)

    task = sys.argv[1]
    skill_name = sys.argv[2] if len(sys.argv) > 2 else None
    
    task_lower = task.lower()
    if any(w in task_lower for w in ["brend", "brand", "logotip", "logo"]):
        product_type = "branding"
    elif any(w in task_lower for w in ["foto", "photo", "fotosessiya", "avatar"]):
        product_type = "media"
    elif any(w in task_lower for w in ["igra", "game", "play"]):
        product_type = "game"
    else:
        product_type = "web"

    words = task.replace('"', '').replace("'", "").split()[:3]
    project_name = "_".join(words).lower() + "_project"
    project_dir = f"./Projects/{project_name}"

    print("=" * 60)
    print("ATLAS DIGITAL STUDIO")
    if BRAIN_READY:
        print("[Brain] Graphify + Memory активны")
    print("=" * 60)
    print(f"\nTask: {task}")
    print(f"Type: {product_type}\n")

    if product_type == "branding":
        print("--- Branding Agent ---")
        run_subprocess("Agents/branding_agent.py", [task, project_dir], "branding")
    elif product_type == "media":
        print("--- Media Agent ---")
        run_subprocess("Agents/media_agent.py", [task, project_dir], "media")
    else:
        print("--- Executive ---")
        run_subprocess("Agents/executive.py", [task], "executive")
        
        print("\n--- Brief Agent ---")
        run_subprocess("Agents/brief.py", [task, project_dir], "brief")
        
        print("\n--- Image Generator ---")
        tasks_file = Path(project_dir) / "image_tasks.json"
        if tasks_file.exists():
            tasks = json.loads(tasks_file.read_text(encoding="utf-8"))
            if tasks:
                run_subprocess("Agents/image_generator.py", [json.dumps(tasks), project_dir], "image_generator")
        
        print("\n--- Web Developer ---")
        skill = skill_name or "product_showcase"
        run_subprocess("Agents/developer.py", [task, project_dir, skill], "developer")

    print("\n" + "=" * 60)
    print("✅ ATLAS COMPLETE")
    print("=" * 60)
    print(f"\nProject: {Path(project_dir).absolute()}")

if __name__ == "__main__":
    main()
'@

Set-Content -Path main.py -Value $code -Encoding UTF8
Напиши: готово + вывод.

User:
готово

Kimi:
Шаг 6: Тест Brain — диагностика
Выполни в PowerShell (в папке Atlas):
python Config\llm_client.py --diagnose
Напиши: готово + весь вывод. Это покажет, построился ли граф и видит ли система контекст.

User:
PS C:> python Config_client.py –diagnose
ATLAS BRAIN DIAGNOSTIC
[Graphify] Available: True [Graphify] Перестраиваю knowledge graph… Traceback (most recent call last): File “C:_client.py”, line 96, in  diagnose() ~~~~~~~~^^ File “C:_client.py”, line 77, in diagnose build_graph() ~~~~~~~~~~~^^ File “C:_bridge.py”, line 204, in build_graph _bridge.build(force=force) ~~~~~~~~~~~~~^^^^^^^^^^^^^ File “C:_bridge.py”, line 45, in build result = subprocess.run( [“graphify”, “.”, “–output”, str(GRAPHIFY_OUT), “–code-only”], …<2 lines>… text=True ) File “C:-64.py”, line 555, in run with Popen(*popenargs, **kwargs) as process: ~~~^^^^^^^^^^^^^^^^^^^^^^ File “C:-64.py”, line 1039, in init self._execute_child(args, executable, preexec_fn, close_fds,~~~~~~~~~~~~~~~~~^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ pass_fds, cwd, env, ^^^^^^^^^^^^^^^^^^^ …<5 lines>… gid, gids, uid, umask, ^^^^^^^^^^^^^^^^^^^^^^ start_new_session, process_group) ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ File “C:-64.py”, line 1553, in _execute_child hp, ht, pid, tid = _winapi.CreateProcess(executable, args, ~~~~~~~~~~~~~~~~~~~~~^^^^^^^^^^^^^^^^^^ # no special security ^^^^^^^^^^^^^^^^^^^^^ …<4 lines>… cwd, ^^^^ startupinfo) ^^^^^^^^^^^^ FileNotFoundError: [WinError 2] Не удается найти указанный файл PS C:>

Kimi:
Шаг 7: Фикс — graphify не в PATH
Нужно найти, где pip установил graphify.exe. Выполни:
Get-ChildItem -Path "C:\Users\diman\AppData\Local\Python" -Recurse -Filter "graphify.exe" -ErrorAction SilentlyContinue
Напиши: готово + путь, если нашёл. Если ничего не нашёл — тоже напиши, сделаем fallback.

User:
PS C:> Get-ChildItem -Path “C:” -Recurse -Filter “graphify.exe” -ErrorAction SilentlyContinue
Каталог: C:\Users\diman\AppData\Local\Python\pythoncore-3.14-64\Scripts
PS C:>

Kimi:
Шаг 8: Фикс graphify_bridge.py — полный путь к graphify.exe
Выполни в PowerShell (в папке Atlas):
$code = @'
"""Graphify Bridge — адаптер между Atlas и Graphify knowledge graph."""
import json
import subprocess
import time
from pathlib import Path
from typing import List, Dict, Any, Optional

BASE_DIR = Path(__file__).parent.parent
GRAPHIFY_OUT = BASE_DIR / "graphify-out"
GRAPH_JSON = GRAPHIFY_OUT / "graph.json"
GRAPH_REPORT = GRAPHIFY_OUT / "GRAPH_REPORT.md"

# Полный путь к graphify.exe (pip установил сюда)
GRAPHIFY_EXE = r"C:\Users\diman\AppData\Local\Python\pythoncore-3.14-64\Scripts\graphify.exe"

class GraphifyBridge:
    """Управляет graphify: запуск, чтение, запросы."""
    
    def __init__(self):
        self._graph: Optional[Dict] = None
        self._report: Optional[str] = None
    
    def _needs_rebuild(self) -> bool:
        """Проверяет, изменились ли файлы проекта с момента последнего графа."""
        if not GRAPH_JSON.exists():
            return True
        graph_mtime = GRAPH_JSON.stat().st_mtime
        latest = 0
        for f in BASE_DIR.rglob("*"):
            if f.is_file() and ".git" not in str(f) and "graphify-out" not in str(f):
                try:
                    mtime = f.stat().st_mtime
                    if mtime > latest:
                        latest = mtime
                except:
                    pass
        return latest > graph_mtime
    
    def build(self, force: bool = False):
        """Запускает graphify если нужно."""
        if not force and not self._needs_rebuild():
            print("[Graphify] Граф актуален, пропускаю.")
            return
        
        print("[Graphify] Перестраиваю knowledge graph...")
        GRAPHIFY_OUT.mkdir(exist_ok=True)
        
        # Проверяем наличие graphify.exe
        if not Path(GRAPHIFY_EXE).exists():
            print(f"[!] graphify.exe не найден: {GRAPHIFY_EXE}")
            print("[Graphify] Fallback: ручная индексация...")
            self._build_fallback()
            return
        
        result = subprocess.run(
            [GRAPHIFY_EXE, ".", "--output", str(GRAPHIFY_OUT), "--code-only"],
            cwd=str(BASE_DIR),
            capture_output=True,
            text=True
        )
        
        if result.returncode != 0:
            print(f"[!] Graphify ошибка: {result.stderr[:500]}")
            self._build_fallback()
        else:
            print("[Graphify] Граф построен.")
            self._load()
    
    def _build_fallback(self):
        """Если graphify не работает — ручная индексация."""
        print("[Graphify] Fallback: ручная индексация...")
        nodes = []
        edges = []
        
        for f in BASE_DIR.rglob("*.py"):
            rel = str(f.relative_to(BASE_DIR)).replace("\\", "/")
            parts = f.relative_to(BASE_DIR).parts
            nodes.append({
                "id": rel,
                "type": "file",
                "label": f.name,
                "community": parts[0] if parts else "root"
            })
        
        for node in nodes:
            fpath = BASE_DIR / node["id"]
            try:
                content = fpath.read_text(encoding="utf-8", errors="ignore")
                for line in content.split("\n"):
                    line = line.strip()
                    if line.startswith("import ") or line.startswith("from "):
                        parts = line.split()
                        if len(parts) >= 2:
                            target = parts[1].split(".")[0]
                            if not target.startswith(("os", "sys", "json", "time", "pathlib", "typing")):
                                for n in nodes:
                                    if target in n["label"].replace(".py", ""):
                                        edges.append({
                                            "source": node["id"],
                                            "target": n["id"],
                                            "relation": "imports",
                                            "confidence": "EXTRACTED"
                                        })
            except:
                pass
        
        self._graph = {"nodes": nodes, "edges": edges}
        GRAPHIFY_OUT.mkdir(exist_ok=True)
        GRAPH_JSON.write_text(json.dumps(self._graph, indent=2, ensure_ascii=False), encoding="utf-8")
    
    def _load(self):
        """Загружает graph.json."""
        if GRAPH_JSON.exists():
            self._graph = json.loads(GRAPH_JSON.read_text(encoding="utf-8"))
        if GRAPH_REPORT.exists():
            self._report = GRAPH_REPORT.read_text(encoding="utf-8")
    
    def get_context(self, query: str, max_nodes: int = 15) -> str:
        """Возвращает релевантный контекст из графа для LLM."""
        if self._graph is None:
            self._load()
            if self._graph is None:
                return ""
        
        query_lower = query.lower()
        nodes = self._graph.get("nodes", [])
        edges = self._graph.get("edges", [])
        
        relevant = []
        for node in nodes:
            score = 0
            label = node.get("label", "").lower()
            community = node.get("community", "").lower()
            
            if any(word in label for word in query_lower.split()):
                score += 10
            if any(word in community for word in query_lower.split()):
                score += 5
            if "skill" in query_lower and "skill" in label:
                score += 8
            if "agent" in query_lower and "agent" in label:
                score += 8
            if "developer" in query_lower and "developer" in label:
                score += 8
            
            if score > 0:
                relevant.append((score, node))
        
        relevant.sort(key=lambda x: x[0], reverse=True)
        top_nodes = [n for _, n in relevant[:max_nodes]]
        
        top_ids = {n["id"] for n in top_nodes}
        neighbor_edges = []
        for edge in edges:
            if edge.get("source") in top_ids or edge.get("target") in top_ids:
                neighbor_edges.append(edge)
                for node in nodes:
                    if node["id"] == edge.get("source") or node["id"] == edge.get("target"):
                        if node not in top_nodes:
                            top_nodes.append(node)
        
        lines = ["=== ATLAS PROJECT STRUCTURE (from Graphify) ===", ""]
        
        communities = {}
        for node in top_nodes:
            comm = node.get("community", "other")
            if comm not in communities:
                communities[comm] = []
            communities[comm].append(node)
        
        for comm, comm_nodes in sorted(communities.items()):
            lines.append(f"📁 {comm}/")
            for node in comm_nodes[:5]:
                lines.append(f"  • {node.get('label', node['id'])} ({node.get('type', 'file')})")
            if len(comm_nodes) > 5:
                lines.append(f"  ... и ещё {len(comm_nodes)-5}")
            lines.append("")
        
        if neighbor_edges:
            lines.append("=== КЛЮЧЕВЫЕ СВЯЗИ ===")
            for edge in neighbor_edges[:10]:
                lines.append(f"  {edge.get('source', '?')} --[{edge.get('relation', '?')}]--> {edge.get('target', '?')}")
        
        if self._report:
            lines.append("")
            lines.append("=== PROJECT WIKI ===")
            lines.append(self._report[:1500])
            lines.append("...")
        
        return "\n".join(lines)
    
    def find_skill(self, skill_name: str) -> Optional[Dict]:
        if self._graph is None:
            self._load()
        for node in self._graph.get("nodes", []):
            if skill_name.lower() in node.get("label", "").lower():
                return node
        return None
    
    def get_file_neighbors(self, file_path: str) -> List[str]:
        if self._graph is None:
            self._load()
        neighbors = []
        for edge in self._graph.get("edges", []):
            if edge.get("source") == file_path:
                neighbors.append(f"{edge.get('relation')} -> {edge.get('target')}")
            elif edge.get("target") == file_path:
                neighbors.append(f"{edge.get('source')} -> {edge.get('relation')}")
        return neighbors

_bridge = GraphifyBridge()

def build_graph(force: bool = False):
    _bridge.build(force=force)

def get_context(query: str, max_nodes: int = 15) -> str:
    return _bridge.get_context(query, max_nodes)

def find_skill(skill_name: str) -> Optional[Dict]:
    return _bridge.find_skill(skill_name)

def get_file_neighbors(file_path: str) -> List[str]:
    return _bridge.get_file_neighbors(file_path)

if __name__ == "__main__":
    build_graph(force=True)
    print(get_context("skills web", max_nodes=10))
'@

Set-Content -Path Brain/graphify_bridge.py -Value $code -Encoding UTF8
Напиши: готово + вывод.

User:
готово

Kimi:
Шаг 9: Перезапуск диагностики Brain
Выполни в PowerShell (в папке Atlas):
python Config\llm_client.py --diagnose
Напиши: готово + весь вывод.

User:
PS C:> python Config_client.py –diagnose
ATLAS BRAIN DIAGNOSTIC
[Graphify] Available: True [Graphify] Перестраиваю knowledge graph… [Graphify] Граф построен. Traceback (most recent call last): File “C:_client.py”, line 96, in  diagnose() ~~~~~~~~^^ File “C:_client.py”, line 78, in diagnose ctx = graphify_context(“skills web”, max_nodes=5) File “C:_bridge.py”, line 217, in get_context return _bridge.get_context(query, max_nodes) ~~~~~~~~~~~~~~~~~~~^^^^^^^^^^^^^^^^^^ File “C:_bridge.py”, line 133, in get_context community = node.get(“community”, ““).lower() ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ AttributeError: ‘int’ object has no attribute ‘lower’ PS C:>

Kimi:
Шаг 10: Фикс — community может быть int в graph.json
Выполни в PowerShell (в папке Atlas):
$code = @'
"""Graphify Bridge — адаптер между Atlas и Graphify knowledge graph."""
import json
import subprocess
from pathlib import Path
from typing import List, Dict, Any, Optional

BASE_DIR = Path(__file__).parent.parent
GRAPHIFY_OUT = BASE_DIR / "graphify-out"
GRAPH_JSON = GRAPHIFY_OUT / "graph.json"
GRAPH_REPORT = GRAPHIFY_OUT / "GRAPH_REPORT.md"

GRAPHIFY_EXE = r"C:\Users\diman\AppData\Local\Python\pythoncore-3.14-64\Scripts\graphify.exe"

class GraphifyBridge:
    def __init__(self):
        self._graph: Optional[Dict] = None
        self._report: Optional[str] = None
    
    def _needs_rebuild(self) -> bool:
        if not GRAPH_JSON.exists():
            return True
        graph_mtime = GRAPH_JSON.stat().st_mtime
        latest = 0
        for f in BASE_DIR.rglob("*"):
            if f.is_file() and ".git" not in str(f) and "graphify-out" not in str(f):
                try:
                    mtime = f.stat().st_mtime
                    if mtime > latest:
                        latest = mtime
                except:
                    pass
        return latest > graph_mtime
    
    def build(self, force: bool = False):
        if not force and not self._needs_rebuild():
            print("[Graphify] Граф актуален, пропускаю.")
            return
        
        print("[Graphify] Перестраиваю knowledge graph...")
        GRAPHIFY_OUT.mkdir(exist_ok=True)
        
        if not Path(GRAPHIFY_EXE).exists():
            print(f"[!] graphify.exe не найден: {GRAPHIFY_EXE}")
            self._build_fallback()
            return
        
        result = subprocess.run(
            [GRAPHIFY_EXE, ".", "--output", str(GRAPHIFY_OUT), "--code-only"],
            cwd=str(BASE_DIR),
            capture_output=True,
            text=True
        )
        
        if result.returncode != 0:
            print(f"[!] Graphify ошибка: {result.stderr[:500]}")
            self._build_fallback()
        else:
            print("[Graphify] Граф построен.")
            self._load()
    
    def _build_fallback(self):
        print("[Graphify] Fallback: ручная индексация...")
        nodes = []
        edges = []
        
        for f in BASE_DIR.rglob("*.py"):
            rel = str(f.relative_to(BASE_DIR)).replace("\\", "/")
            parts = f.relative_to(BASE_DIR).parts
            nodes.append({
                "id": rel,
                "type": "file",
                "label": f.name,
                "community": parts[0] if parts else "root"
            })
        
        for node in nodes:
            fpath = BASE_DIR / node["id"]
            try:
                content = fpath.read_text(encoding="utf-8", errors="ignore")
                for line in content.split("\n"):
                    line = line.strip()
                    if line.startswith("import ") or line.startswith("from "):
                        parts = line.split()
                        if len(parts) >= 2:
                            target = parts[1].split(".")[0]
                            if not target.startswith(("os", "sys", "json", "time", "pathlib", "typing")):
                                for n in nodes:
                                    if target in n["label"].replace(".py", ""):
                                        edges.append({
                                            "source": node["id"],
                                            "target": n["id"],
                                            "relation": "imports",
                                            "confidence": "EXTRACTED"
                                        })
            except:
                pass
        
        self._graph = {"nodes": nodes, "edges": edges}
        GRAPHIFY_OUT.mkdir(exist_ok=True)
        GRAPH_JSON.write_text(json.dumps(self._graph, indent=2, ensure_ascii=False), encoding="utf-8")
    
    def _load(self):
        if GRAPH_JSON.exists():
            self._graph = json.loads(GRAPH_JSON.read_text(encoding="utf-8"))
        if GRAPH_REPORT.exists():
            self._report = GRAPH_REPORT.read_text(encoding="utf-8")
    
    def get_context(self, query: str, max_nodes: int = 15) -> str:
        if self._graph is None:
            self._load()
            if self._graph is None:
                return ""
        
        query_lower = query.lower()
        nodes = self._graph.get("nodes", [])
        edges = self._graph.get("edges", [])
        
        relevant = []
        for node in nodes:
            score = 0
            label = str(node.get("label", "")).lower()
            # FIX: community может быть int в graphify output
            community_raw = node.get("community", "")
            community = str(community_raw).lower() if community_raw is not None else ""
            
            if any(word in label for word in query_lower.split()):
                score += 10
            if any(word in community for word in query_lower.split()):
                score += 5
            if "skill" in query_lower and "skill" in label:
                score += 8
            if "agent" in query_lower and "agent" in label:
                score += 8
            if "developer" in query_lower and "developer" in label:
                score += 8
            
            if score > 0:
                relevant.append((score, node))
        
        relevant.sort(key=lambda x: x[0], reverse=True)
        top_nodes = [n for _, n in relevant[:max_nodes]]
        
        top_ids = {n["id"] for n in top_nodes}
        neighbor_edges = []
        for edge in edges:
            if edge.get("source") in top_ids or edge.get("target") in top_ids:
                neighbor_edges.append(edge)
                for node in nodes:
                    if node["id"] == edge.get("source") or node["id"] == edge.get("target"):
                        if node not in top_nodes:
                            top_nodes.append(node)
        
        lines = ["=== ATLAS PROJECT STRUCTURE (from Graphify) ===", ""]
        
        communities = {}
        for node in top_nodes:
            comm_raw = node.get("community", "other")
            comm = str(comm_raw) if comm_raw is not None else "other"
            if comm not in communities:
                communities[comm] = []
            communities[comm].append(node)
        
        for comm, comm_nodes in sorted(communities.items()):
            lines.append(f"📁 {comm}/")
            for node in comm_nodes[:5]:
                lines.append(f"  • {node.get('label', node['id'])} ({node.get('type', 'file')})")
            if len(comm_nodes) > 5:
                lines.append(f"  ... и ещё {len(comm_nodes)-5}")
            lines.append("")
        
        if neighbor_edges:
            lines.append("=== КЛЮЧЕВЫЕ СВЯЗИ ===")
            for edge in neighbor_edges[:10]:
                lines.append(f"  {edge.get('source', '?')} --[{edge.get('relation', '?')}]--> {edge.get('target', '?')}")
        
        if self._report:
            lines.append("")
            lines.append("=== PROJECT WIKI ===")
            lines.append(self._report[:1500])
            lines.append("...")
        
        return "\n".join(lines)
    
    def find_skill(self, skill_name: str) -> Optional[Dict]:
        if self._graph is None:
            self._load()
        for node in self._graph.get("nodes", []):
            if skill_name.lower() in str(node.get("label", "")).lower():
                return node
        return None
    
    def get_file_neighbors(self, file_path: str) -> List[str]:
        if self._graph is None:
            self._load()
        neighbors = []
        for edge in self._graph.get("edges", []):
            if edge.get("source") == file_path:
                neighbors.append(f"{edge.get('relation')} -> {edge.get('target')}")
            elif edge.get("target") == file_path:
                neighbors.append(f"{edge.get('source')} -> {edge.get('relation')}")
        return neighbors

_bridge = GraphifyBridge()

def build_graph(force: bool = False):
    _bridge.build(force=force)

def get_context(query: str, max_nodes: int = 15) -> str:
    return _bridge.get_context(query, max_nodes)

def find_skill(skill_name: str) -> Optional[Dict]:
    return _bridge.find_skill(skill_name)

def get_file_neighbors(file_path: str) -> List[str]:
    return _bridge.get_file_neighbors(file_path)

if __name__ == "__main__":
    build_graph(force=True)
    print(get_context("skills web", max_nodes=10))
'@

Set-Content -Path Brain/graphify_bridge.py -Value $code -Encoding UTF8
Напиши: готово + вывод.

User:
готово

Kimi:
Шаг 11: Перезапуск диагностики Brain
Выполни в PowerShell (в папке Atlas):
python Config\llm_client.py --diagnose
Напиши: готово + весь вывод.

User:
PS C:> python Config_client.py –diagnose
ATLAS BRAIN DIAGNOSTIC
[Graphify] Available: True [Graphify] Перестраиваю knowledge graph… [Graphify] Граф построен. Context sample: === ATLAS PROJECT STRUCTURE (from Graphify) ===
📁 1/ • Developer Agent — universal. Ispolzuet lyuboy skill iz papki Skills/. Avtomatich (file) • load_skill() (file)
📁 13/ • skill.json (file)
📁 9/ • skills_manager.py (file) • list_skills() (file) …
[OLLAMA] Models: [‘qwen2.5-coder:7b’, ‘qwen2.5-coder:3b’, ‘deepseek-coder:6.7b’, ‘qwen2.5-coder:latest’] PS C:>

Kimi:
Шаг 12: Тест — ask_llm с авто-контекстом из Graphify
Выполни в PowerShell (в папке Atlas):
python Config\llm_client.py
Это запустит тестовый вызов: спросит у LLM «Какие skills доступны для создания сайтов?» с автоподгрузкой контекста из графа.
Напиши: готово + весь вывод (может быть длинным, контекст + ответ модели).

User:
PS C:> python Config_client.py [Brain] executive → Ollama (qwen2.5-coder:3b) Основные skills для создания сайтов могут включать в себя:
Frontend Development (веб-разработка)
HTML/CSS
JavaScript
React.js/Next.js/Vue.js/Angular.js
Bootstrap/Sass/Less
Responsive Design
Backend Development (серверное разработка)
Node.js/Express.js
Python/Django/FastAPI
PHP/Laravel/CodeIgniter
Java/Spring Boot
Ruby on Rails
Full-Stack Development
Микросервисы и microservices architecture
Docker/CircleCI/TravisCI
Git/GitHub/GitLab
Database Management
MySQL/MariaDB/postgreSQL
MongoDB (NoSQL database)
Redis for caching
Version Control
GitHub Actions
GitLab CI
Bitbucket Pipelines
DevOps and Infrastructure as Code (IaC)
Terraform
Ansible
Kubernetes (Docker container orchestration)
Web Security
OWASP Top 10 vulnerabilities
Security best practices
Penetration Testing
Content Management Systems (CMS)
WordPress/Drupal/Joomla
Static Site Generators (Gatsby, Jekyll)
E-commerce Development
WooCommerce
Shopify
Magento
Mobile Web Development (for mobile-first approach)
React Native
Flutter
Ionic
SEO and Content Strategy
Keyword research
On-page optimization
Search Engine Marketing (SEM)
Payment Gateway Integration
Stripe, PayPal, Braintree
Custom payment processing
Analytics and Monitoring
Google Analytics
New Relic
AWS CloudWatch
Performance Optimization
Cache optimization techniques
Minification of code/assets
Browser caching strategies
Accessibility and Usability
WCAG (Web Content Accessibility Guidelines)
A/B testing for user experience improvement
Эти skills могут быть использованы в различных контекстах проекта для создания сайтов, включая индивидуальные проекты, микросервисные архитектуры или крупнейшие интернет-платформы. PS C:>

Kimi:
Шаг 13: Улучшить контекст — явно читать skill.json файлы
Сейчас LLM видит структуру файлов, но не понимает, что это skills. Добавим явное чтение skill.json — тогда контекст будет: «Доступные skills: modern_landing (Tailwind), motion_premium (GSAP)…»
Выполни в PowerShell (в папке Atlas):
```powershell $code = @’ “““Graphify Bridge — адаптер между Atlas и Graphify knowledge graph.”“” import json import subprocess from pathlib import Path from typing import List, Dict, Any, Optional
BASE_DIR = Path(file).parent.parent GRAPHIFY_OUT = BASE_DIR / “graphify-out” GRAPH_JSON = GRAPHIFY_OUT / “graph.json” GRAPH_REPORT = GRAPHIFY_OUT / “GRAPH_REPORT.md”
GRAPHIFY_EXE = r”C:-64.exe”
class GraphifyBridge: def init(self): self._graph: Optional[Dict] = None self._report: Optional[str] = None
def _needs_rebuild(self) -> bool:
    if not GRAPH_JSON.exists():
        return True
    graph_mtime = GRAPH_JSON.stat().st_mtime
    latest = 0
    for f in BASE_DIR.rglob("*"):
        if f.is_file() and ".git" not in str(f) and "graphify-out" not in str(f):
            try:
                mtime = f.stat().st_mtime
                if mtime > latest:
                    latest = mtime
            except:
                pass
    return latest > graph_mtime

def build(self, force: bool = False):
    if not force and not self._needs_rebuild():
        print("[Graphify] Граф актуален, пропускаю.")
        return
    
    print("[Graphify] Перестраиваю knowledge graph...")
    GRAPHIFY_OUT.mkdir(exist_ok=True)
    
    if not Path(GRAPHIFY_EXE).exists():
        print(f"[!] graphify.exe не найден: {GRAPHIFY_EXE}")
        self._build_fallback()
        return
    
    result = subprocess.run(
        [GRAPHIFY_EXE, ".", "--output", str(GRAPHIFY_OUT), "--code-only"],
        cwd=str(BASE_DIR),
        capture_output=True,
        text=True
    )
    
    if result.returncode != 0:
        print(f"[!] Graphify ошибка: {result.stderr[:500]}")
        self._build_fallback()
    else:
        print("[Graphify] Граф построен.")
        self._load()

def _build_fallback(self):
    print("[Graphify] Fallback: ручная индексация...")
    nodes = []
    edges = []
    
    for f in BASE_DIR.rglob("*.py"):
        rel = str(f.relative_to(BASE_DIR)).replace("\\", "/")
        parts = f.relative_to(BASE_DIR).parts
        nodes.append({
            "id": rel,
            "type": "file",
            "label": f.name,
            "community": parts[0] if parts else "root"
        })
    
    for node in nodes:
        fpath = BASE_DIR / node["id"]
        try:
            content = fpath.read_text(encoding="utf-8", errors="ignore")
            for line in content.split("\n"):
                line = line.strip()
                if line.startswith("import ") or line.startswith("from "):
                    parts = line.split()
                    if len(parts) >= 2:
                        target = parts[1].split(".")[0]
                        if not target.startswith(("os", "sys", "json", "time", "pathlib", "typing")):
                            for n in nodes:
                                if target in n["label"].replace(".py", ""):
                                    edges.append({
                                        "source": node["id"],
                                        "target": n["id"],
                                        "relation": "imports",
                                        "confidence": "EXTRACTED"
                                    })
        except:
            pass
    
    self._graph = {"nodes": nodes, "edges": edges}
    GRAPHIFY_OUT.mkdir(exist_ok=True)
    GRAPH_JSON.write_text(json.dumps(self._graph, indent=2, ensure_ascii=False), encoding="utf-8")

def _load(self):
    if GRAPH_JSON.exists():
        self._graph = json.loads(GRAPH_JSON.read_text(encoding="utf-8"))
    if GRAPH_REPORT.exists():
        self._report = GRAPH_REPORT.read_text(encoding="utf-8")

def _get_skills_catalog(self) -> str:
    """Явно читает все skill.json и формирует каталог."""
    lines = ["=== ДОСТУПНЫЕ SKILLS ==="]
    skills_dir = BASE_DIR / "Skills"
    
    # Web skills
    web_dir = skills_dir / "web"
    if web_dir.exists():
        lines.append("\n[WEB]

Было
Стало
llm_client.py — только Ollama
Model Router v2 — 7 провайдеров, авто-fallback, выбор по agent_type
executive.py — зовёт ask_llm(model="7b")
Executive v2 — зовёт ask_llm(agent_type="executive"), пишет план в Memory/context.json
main.py — просто запускает подпроцессы
Orchestrator v2 — читает context.json, сам решает кого запускать и в каком порядке
Агенты не видят друг друга
Context Bridge — Memory/context.json = единая память сессии
Проблема
Почему больно
Я — единственный мозг
Я не вижу твои локальные файлы. Все решения проходят через копипасту
Контекст тонет
50 KB чекпоинта не передают состояние файлов. Я не знаю, какой баг был вчера
Qwen 3B/7B — слабый
Для архитектурных решений нужен Gemini 2.5 Pro / Cerebras, но они не «живут» в проекте
Нет памяти
Агенты не помнят, что делали в прошлый раз. Каждый запуск — с чистого листа
Mode
LastWriteTime
Length
Name
d—– 20.07.2
026 1:23

Brain
pip : Имя “pip” не ра
спознано как имя коман
длета,
функции, файла сценария или выполняемой программы. Проверьте правильность написания имени, а также наличие и правильность пути, после чего повторите попытку.
строка:3 знак:1



+ pip install graphif
yy


+ ~~~



+ CategoryInfo
: ObjectNotFoun
d: (pip
:String) [], CommandNotFoundException
+ FullyQualifiedE
rrorId : CommandNotFou
ndExcep
tion
Mode
LastWriteTime
Length
Name
-a—- 20.07.2
026 1:24
108365
graphify.exe